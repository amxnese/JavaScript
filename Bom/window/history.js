// Getting The Length of The History
console.log(history.length);

// Traversing The History Backwards
history.back();

// Traversing The History Forwards
history.forward();

// Indexing The History
history.go(2); // Two Steps Forward
history.go(-2); // Two Steps Backward